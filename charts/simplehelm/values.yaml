replicaCount: 1

image:
  repository: almuko/simplehelm
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: simplehelm.carservices.kz
      paths:
        - path: /
          pathType: Prefix
